name: Java CI

on:
  push:

jobs:
  printing:
    runs-on: ubuntu-latest
    steps:
      - name : print Hello!
        run : echo "Hello, World!"


      - name : ls command
        run : |
          ls -l
          ls ../
      - name : whoami command
        run : whoami

      - name : pwd where am i
        run : pwd
      - name : find Java
        run : find / -name  "Main.java" 2>/dev/null
        
  
  



